pipeline {
    agent { podman 'python:3.5.1' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
        stage('deploy') {
            steps {
                sh 'python --version'
            }
        }
    }
}
